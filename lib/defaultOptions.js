const defaultOptions = {
    stripTagPrefix: 'data',
    aliasForThis: '$ctrl',
    format: 'html',
    bindToCurlyBraces: false,
    classListToRemove: ['ng-hide']
}

module.exports = defaultOptions
